<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Light peach color -->
    <corners android:radius="15dp" /> <!-- Rounded corners -->
    <stroke
        android:width="1dp"
        android:color="#FFF0E0E0" /> <!-- Light border color -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" /> <!-- Padding inside the EditText -->
</shape>
